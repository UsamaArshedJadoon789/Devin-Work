import React from 'react';
import ReactDOM from 'react-dom/client';
import { ProgressReportPage } from './components/progress-reports/ProgressReportPage';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ProgressReportPage />
  </React.StrictMode>
);
